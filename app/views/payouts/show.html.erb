<p id="notice"><%= notice %></p>

<h1>Запрос на выплату #<%= @payout.id %></h1>

<table>
  <tr>
    <th>Дата-время</th><td><%= @payout.dt %></td>
  </tr>
  <tr>
    <th>Сумма выплаты</th><td><%= @payout.amount %></td>
  </tr>
  <tr>
    <th>Телефон клиента</th><td><%= @payout.phone %></td>
  </tr>
  <tr>
    <th>Номер банковской карты</th><td><%= @payout.client %></td>
  </tr>
  <tr>
    <th>ФИО получателя</th><td><%= @payout.receiver_fio %></td>
  </tr>
  <tr>
    <th>Запрос на выплату</th><td><%= @payout.response_payout %></td>
  </tr>
</table>

<%= link_to 'На главную', root_path %>
